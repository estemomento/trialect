# Trialect-server

> A dialect website server

## Build Setup
With `yarn`

``` bash
# install dependencies
yarn

# start server
yarn start
```

Without `yarn`

``` bash
# install dependencies
npm install

# start server
npm run start
```